[develop]
script_dir=$base/lib/hera_robot

[install]
install_scripts=$base/lib/hera_robot

[options]
package_dir=
    =.
packages=find:
install_requires=
    setuptools
    vosk
    pyaudio

[options.packages.find]
where=.
